/** Reuse taxonomy icon keys — directories share the same Lucide set. */
export { CATEGORY_ICON_KEYS as DIRECTORY_ICON_KEYS } from "@/lib/categories/constants"

export const DIRECTORIES_PAGE_SIZE = 20
